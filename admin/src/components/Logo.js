import React from "react";
import logoUrl from "../assets/logo.svg";

const Logo = () => {
  return <img style={{ height: "60px", paddingLeft: "20px", width: " 150px" }} alt="" src={logoUrl} />;
};

export default Logo;
